# Projects Workspace

Ce dossier contient les projets clients isolés de la démo `Stroopwafelshop`.

Point de départ recommandé :

- copier `projects/client_template` vers `projects/<nom_client>`;
- adapter les fichiers `meltano` et `dbt`;
- lancer `make up`, `make load`, `make bi`, `make check`.
